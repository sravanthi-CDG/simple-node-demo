version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step needed - serving static files"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []
